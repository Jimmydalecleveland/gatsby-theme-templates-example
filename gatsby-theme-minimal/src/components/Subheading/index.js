import React from 'react'

const Subheading = ({ children }) => (
  <h1
    style={{
      background: 'peachpuff',
      color: 'coral',
      fontSize: '24px',
      padding: '8px 0',
      textAlign: 'center',
    }}
  >
    {children}
  </h1>
)

export default Subheading
