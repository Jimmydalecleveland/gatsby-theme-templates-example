import React from 'react'

const InFolder = ({ children }) => (
  <h1
    style={{
      background: 'palevioletred',
      color: 'white',
      fontSize: '48px',
      textAlign: 'center',
    }}
  >
    {children}
  </h1>
)

export default InFolder
