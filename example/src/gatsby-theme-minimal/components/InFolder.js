import React from 'react'
import InFolder from 'gatsby-theme-minimal/src/components/InFolder'

const ShadowedInFolder = ({ children }) => <InFolder>Shadowed in example site: {children}</InFolder>

export default ShadowedInFolder
